import React from "react";

interface ModalProp {
  show: boolean;
  setShow: (event: any) => void;
  setChange?: any;
}

const UploadModal = () => {
  return <div>UploadModal</div>;
};

export default UploadModal;
